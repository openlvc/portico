<?xml version="1.0"?>
<!--
                  Welcome to the Portico Build System

       The Portico build system is an artefact-driven, modular, Ant
       based system. The base framework is split up over a number of
       different build files (base structure, macros, etc...) and is
       designed to be extended rather than modified. The full system
       is introduced and described in the online documentation:
       
       http://porticoproject.org/index.php?title=Building_Portico

                              *** NOTE ***                        
       Do not edit this file unless you wish to fundamentally change
       where the build puts high-level files and directories. If you
       want to provide profile-specific properties, please put these
       at the head of your profile build file. If you want to define
       properties used across profiles, create a separate properties
       file and import it into your various profile builds.
-->
<project name="system.properties">

	<!-- =========================================================== -->
	<!--                      General Settings                       -->
	<!-- =========================================================== -->
	<description>
		This project contains the system-wide properties commonly used across all build profiles
	</description>

	<!-- =========================================================== -->
	<!--                   Project-wide Properties                   -->
	<!-- =========================================================== -->
	<!-- load the environment variables -->
	<property environment="env"/>

	<!-- Build version identification properties (set if not in "build.version") -->
	<property file="build.version"/>        <!-- bring in the build version information -->
	<property name="app.shortname"          value="portico"/> <!-- set in build.version -->
	<property name="app.version"            value="unknown"/> <!-- set in build.version -->
	<property name="app.license"            location="LICENSE.${ant.project.name}"/>
	
	<!-- Platform Determination Properties -->
	<!-- These tasks will initialize a bunch of properties based
	     on the system that the build is currently running on. The
	     various properties and values set are:
	       * ${platform}         : One of "macos, win32, win64, linux32, linux64"
	       * ${platform.macos}   : "true" if we are running on Mac OS X
	       * ${platform.win32}   : "true" if we are running on 32-bit Windows
	       * ${platform.win64}   : "true" if we are running on 64-bit Windows
	       * ${platform.linux32} : "true" if we are running on 32-bit Linux
	       * ${platform.linux64} : "true" if we are running on 64-bit Linux
	-->
	<condition property="platform.macos" value="true"><os family="mac" arch="x86_64"/></condition>
	<condition property="platform.win32" value="true"><os family="winnt" arch="x86_32"/></condition>
	<condition property="platform.win64" value="true"><os family="winnt" arch="x86_64"/></condition>
	<condition property="platform.linux32" value="true"><os name="Linux" arch="x86_32"/></condition>
	<condition property="platform.linux64" value="true"><os name="Linux" arch="x86_64"/></condition>
	<condition property="platform" value="macos"><isset property="platform.macos"/></condition>
	<condition property="platform" value="win32"><isset property="platform.win32"/></condition>
	<condition property="platform" value="win64"><isset property="platform.win64"/></condition>
	<condition property="platform" value="linux32"><isset property="platform.linux32"/></condition>
	<condition property="platform" value="linux32"><isset property="platform.linux64"/></condition>
	
	<!-- =========================================================== -->
	<!--                     Compiler Properties                     -->
	<!-- =========================================================== -->
	<!-- java compiler properties -->
	<property name="java.compiler.source" value="1.6"/>
	<property name="java.compiler.target" value="1.6"/>

	<!-- =========================================================== -->
	<!--          Locations, Resources and Build Directories         -->
	<!-- =========================================================== -->
	<!-- Location Properties -->
	<property name="lib.dir"                   location="lib"/>
	<property name="src.dir"                   location="src"/>
	<property name="java.src.dir"              location="${src.dir}/java"/>
	<property name="cpp.src.dir"               location="${src.dir}/cpp"/>

	<!-- Resources Directories -->
	<property name="resources.dir"             location="resources"/>
	<property name="resources.jars.dir"        location="${resources.dir}/jars"/>
	<property name="resources.dist.dir"        location="${resources.dir}/dist"/>
	<property name="resources.dist.common.dir" location="${resources.dist.dir}/common"/>
	<property name="resources.installers.dir"  location="${resources.dir}/installers"/>
	
	<!-- Generated Build and Distribution Properties -->
	<property name="build.dir"              location="build"/>
	<property name="dist.name"              value="${app.shortname}-${app.version}"/>
	
	
	<property name="dist.dir"               location="dist"/>
	<property name="dist.sandbox.dir"       location="${dist.dir}/${dist.name}"/>
	<property name="dist.lib.dir"           location="${dist.sandbox.dir}/lib"/>
	<property name="dist.zip.file"          location="${dist.dir}/${dist.name}-${platform}.zip"/>
	<property name="dist.tgz.file"          location="${dist.dir}/${dist.name}-${platform}.tar.gz"/>

</project>
